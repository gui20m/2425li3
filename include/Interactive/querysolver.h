#ifndef QUERYSOLVER_H
#define QUERYSOLVER_H

#include "Interactive/interactive.h"
#include <ncurses.h>

/// @brief Função para processar a query1
/// @param setts Estrutura que guarda as settings
void process_query1(SETTINGS setts);

/// @brief Função para processar a query2
/// @param setts Estrutura que guarda as settings
void process_query2(SETTINGS setts);

/// @brief Função para processar a query3
/// @param setts Estrutura que guarda as settings
void process_query3(SETTINGS setts);

/// @brief Função para processar a query4
/// @param setts Estrutura que guarda as settings
void process_query4(SETTINGS setts);

/// @brief Função para processar a query5
/// @param setts Estrutura que guarda as settings
void process_query5(SETTINGS setts);

/// @brief Função para processar a query6
/// @param setts Estrutura que guarda as settings
void process_query6(SETTINGS setts);

#endif